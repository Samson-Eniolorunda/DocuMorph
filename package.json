{
  "name": "documorph",
  "version": "1.0.0",
  "description": "DocuMorph (personal project) — static site preview using a local server.",
  "scripts": {
    "start": "serve"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/blob": "^1.0.0"
  }
}
